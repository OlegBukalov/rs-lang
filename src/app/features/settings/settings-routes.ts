import { Routes } from '@angular/router';
import { TextBookSettingsComponent } from './text-book-settings.component';

export const settingsRoutes: Routes = [
  {
    path: '',
    component: TextBookSettingsComponent,
  },
];
